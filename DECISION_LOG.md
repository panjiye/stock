# 设计决策记录

## 2026-08-06：买入/卖出价改按最近交易日匹配

**决策**：新增 `get_nearest_price()`（批量）与 `get_nearest_trade_price()`（单股票）函数，买入 `get_open_price()` 与卖出 `get_close_price()` 统一改为「target_date 当日或之前最近一个交易日」从 `daily_price_qfq` 取价，取代原先的精确日期匹配。

**依据**：coverage.csv 揭示 85 个理论季度中有 28 期因卖出日（季度末)是非交易日、精确日期查询落空而被整期跳过（EMPTY_CLOSE），导致回测时间轴非连续。真实交易中，非交易日顺延至最近交易日成交是常规做法，属数据口径的合理修复。

**边界**：仅修复交易日期匹配，不改 `factor_score`、`technical_factor`、`strategy_pipeline`，不改数据库，不改收益/权重计算逻辑。

**影响**：EMPTY_CLOSE 由 28 → 0；实际调仓期 56 → 84；equity 季度时间轴恢复连续（90~92 天均匀间隔）。最终资产由 15,389,833.64 → 17,785,136.46，反映纳入完整季度后的口径。coverage.csv 仍保留边界期 EMPTY_STOCKS（回测起点无因子），继续作为数据缺口监控依据。

## 2026-08-06：缺失季度显式记录，禁止静默跳过

**决策**：在 `run_backtest` 中对所有理论季度调仓期一律生成 coverage 记录，连续输出 `coverage.csv`（字段：rebalance_date、has_factor_score、stock_count、status、actual_rebalance），并显式打印跳过原因。

**依据**：此前期缺失季度通过 `continue` 被静默丢弃，导致回测时间轴非连续却无任何提示，收益/回撤/Sharpe 的口径失真不可见。将缺口透明化是判断回测可信范围的前提。

**影响**：coverage.csv 揭示真实缺口主因——85 个理论季度中 28 期因"卖出价采用季度末当日精确匹配 daily_price_qfq、当季度末日为非交易日"被整体跳过（EMPTY_CLOSE）。该逻辑属"输出与诊断层"改动，不再触碰因子与交易执行逻辑。V5.1 修复"卖出日取最近交易日"时，可用 coverage.csv 验证修复效果。

## 2026-08-06：参数快照补齐可复现声明

**决策**：为 `params.json` 补齐回测截止日、调仓周期、手续费率、滑点、因子版本、数据库版本与 git commit。

**依据**：原参数快照仅含初始资金、TOPN 等 5 项，无法满足"可复现环境快照"声明，也无法支撑后续对比。

**影响**：`git commit` 与 `database_version` 通过动态收集（git rev-parse + 各核心表 max date）写入，不新增表、不改库。当前回测尚未应用成本模型，手续费/滑点如实标注为 0.0，避免虚假精确。

## 2026-08-06：回测结果仅作流程验证声明

**决策**：在 `REPORT.md` 顶部明确"当前回测结果仅用于流程验证，不构成任何策略有效性或投资结论"。

**依据**：技术因子未来函数未修复 + 回测时间轴非连续双重前提下，收益/回撤/Sharpe 不能作为策略有效性证据。

**影响**：任何后续读取 V5.0 Baseline 报告的人都会被明确引导到"可信基线尚未达成"的语境，防止误用数字下结论。

## 2026-08-06：保持 V5 渐进迁移路线

**决策**：不重新设计数据、因子、策略和回测架构；继续以 Writer/Query Layer、Factor Layer、Strategy Layer、Backtest、Risk Layer、Portfolio Layer 的既定顺序推进。

**依据**：项目已经拥有完整数据资产、48 万级因子数据和可复现的季度因子回测产物。一次性替换会破坏历史对照，且无法区分架构变动与策略变化的影响。

**影响**：后续改动必须以小步适配、明确验证、保留基准为原则。

## 2026-08-06：以 `scripts/backtest_factor.py` 作为当前 V5 基准入口

**决策**：将 `factor_score → select_strategy_stocks() → scripts/backtest_factor.py` 定义为当前已验证的 V5 基准链路。

**依据**：该路径已有 `results/v5/baseline/` 下的 equity、trades 及对应的 56 个季度回测产物。相比之下，旧版通用 engine 与 V4.2 engine 依赖缺失模块或历史路径。

**影响**：Risk Layer 首先应消费该基准链路的标准化输出；旧 engine 只作为历史实现参考，待单独验证后再纳入。

## 2026-08-06：暂不重构 Portfolio、Broker 与成本模型

**决策**：不对 `backtest/portfolio.py`、`backtest/broker.py`、`backtest/cost.py` 做大规模改造。

**依据**：这些模块职责清晰，分别处理持仓、成交与 A 股交易成本；当前主要问题不在其内部，而在回测入口、风险模块接入和结果标准化。

**影响**：新增风控或组合逻辑应通过明确接口接入，而非把业务规则散落到基础成交模块。

## 2026-08-06：将 V5.0 Beta 基线优先于 V5.1 技术因子重构

**决策**：当前阶段先完成一个可运行、可测试、可比较的 V5.0 Beta 基线，而不是先推进 V5.1 技术因子重构。

**依据**：项目目前已经具备数据层、因子层、策略层和回测层，但尚未形成一个稳定可复现的 data → factor → strategy → backtest → result 闭环。没有 baseline，后续任何优化都无法判断收益是提升还是损失。

**影响**：后续工作将以“固化主链路、修复运行问题、产出可展示、可复盘、可分析的 V5.0 Beta 基线”为优先级。V5.1 的技术因子未来函数修复、v51 表新增、analysis 模块大规模拆分和数据库结构重设计都将延后，作为后续阶段任务。

## 2026-08-06：保留原始财报与独立标准化层

**决策**：`financial_profit` 保留外部来源原始格式，百分比转换集中在 `analysis/financial_normalize.py`，生成 `financial_profit_normalized`。

**依据**：EastMoney 历史百分比字段存在小数与百分数两种格式；在下载层转换会混淆原始数据与处理规则，并可能重复除以 100。

**影响**：下载脚本不得新增相同的百分比换算；财务字段映射应先核验语义，再调整标准化或下游因子。

## 2026-08-06：不重建现有 SQLite 数据库

**决策**：不直接运行早期建表脚本重建或清空 `database/stock.db`。

**依据**：真实数据库已积累约 7.7GB 数据，其 schema 包含下载日志复合主键、唯一索引和后续新增字段，与 `scripts/create_tables.py` 并不完全一致。

**影响**：任何 schema 变更必须先导出真实 schema、评估下游依赖并使用可回滚的迁移方式。

## 2026-08-06：历史代码和历史结果只读保留

**决策**：`archive/` 下的历史结果和历史报告不作为当前功能开发的直接修改对象。

**依据**：这些资产用于对照 V4.2/V5 不同阶段的研究结果；直接覆盖会丧失结果可追溯性。

**影响**：新结果应写入独立、版本明确的标准目录，并记录回测参数、数据截止日和因子版本。

## 2026-08-06：以标准结果目录作为 V5.0 Beta 交付口径

**决策**：将 [results/v5/v5.0_baseline](results/v5/v5.0_baseline) 作为当前 V5.0 Beta 的唯一标准输出口径，后续任何基线展示、复盘和对比都应基于该目录。

**依据**：当前主回测已经能生成完整回测结果，因此需要让结果产出、报告、图表和参数快照形成统一交付格式，而不是散落于根目录或临时文件。

**影响**：后续工作应以该目录为准，不再以根目录文件作为交付物。

## 2026-08-06：主线与历史资产物理隔离

**决策**：将已被 V5 替代的下载脚本、V4.1/V4.2 回测与结果、一次性迁移工具移入 `archive/`；删除仅为空占位、缓存、日志或已确认完全重复的文件。

**依据**：主目录同时存在多套数据下载、回测和结果入口，部分脚本依赖已经失效的模块或硬编码历史结果路径，容易误导后续维护和 AI 判断当前运行链路。

**边界**：不清理 `database/stock.db`、`.venv/`、项目快照包，也不删除具有研究价值的历史源码和结果。归档资产只读保留，不再作为当前 V5 入口使用。

**影响**：当前主线限定为 `data → analysis → strategy → scripts/backtest_factor.py`；Risk Layer 必须以 V5 标准化结果重新实现，而非直接修补归档脚本。
